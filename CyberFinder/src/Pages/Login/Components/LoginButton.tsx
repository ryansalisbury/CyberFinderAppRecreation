import { Button, Stack } from "@mui/material";
import { useState } from "react";

interface Props {
  submit: () => void;
}

const LoginButton = () => {
  return <></>;
};

export default LoginButton;
